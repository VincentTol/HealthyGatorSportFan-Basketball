# Copy this file to .env and fill in the values.
# For local dev you can use SQLite by leaving DATABASE_NAME empty (see below).

# --- Required for Django ---
# Generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-here

# --- Optional: local dev defaults ---
DEBUG=true
PORT=8000

# --- PostgreSQL (local dev): only needed if you use PostgreSQL ---
# If these are empty/missing, the project uses SQLite for local dev (no Postgres needed).
DATABASE_NAME=healthygator_db
DATABASE_USER=postgres
DATABASE_PASSWORD=your-postgres-password
DATABASE_HOST=localhost
DATABASE_PORT=5432

# --- Redis: optional for local (defaults to localhost) ---
# REDIS_URL=redis://localhost:6379/0

# --- Optional: external APIs (for Celery / basketball poll) ---
# COLLEGE_BASKETBALL_API_KEY=your-cbb-api-key  # collegebasketballdata.com (schedule)
# Live scores use NCAA API (no key): https://ncaa-api.henrygd.me

# --- Basketball poll: team to match on live scoreboard (default: Florida) ---
# CURR_TEAM=Florida

# --- Ngrok: configure on ngrok.com, then run: ngrok config add-authtoken <token> ---
# The auth token is set in ngrok's config, not in .env. Add your ngrok URL to ALLOWED_HOSTS in settings.py.
